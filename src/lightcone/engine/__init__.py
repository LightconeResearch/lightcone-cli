"""The lightcone execution engine.

Rebuild in progress: the engine is being re-added layer by layer on top of
the normative design spec. Layer 1 is project scaffolding only —
:mod:`lightcone.engine.project` (convergence) and
:mod:`lightcone.engine.templates` (the scaffolded files).
"""

from __future__ import annotations
